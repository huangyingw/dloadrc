version: '2.1'

services:
    dbuntu:
        build:
            context: .
            dockerfile: Dockerfile
        image: huangyingw/dvim
        volumes:
            - ./var/logs/:/var/logs/
        environment:
            - FILENAME="~/.bashrc"
